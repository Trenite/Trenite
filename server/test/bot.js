const assert = require("assert");
const test = require("../discord-test.js");

describe("Bot", () => {
	it("should start without any errors", async function () {
		await new Promise((res) => setTimeout(res, 5000));
	});
});
